package com.thenexusreborn.nexuscore.multicraft.data;

public class ServerInfo implements MulticraftObject {
    public int memory,startMemory, port, autoStart, defaultLevel, daemonId, announceSave, kickDelay, suspended, autosave, players, id, diskQuota;
    public String ip, world, jarfile,jardir, template, setup, prevJarfile, params, crashCheck, domain, name, dir;
}